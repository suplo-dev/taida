# Nuxt dev/build outputs
.data
.nuxt
.nitro
.cache
.output
dist

# Node dependencies
node_modules

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
# `.env.*` above would otherwise swallow this one too — it is a template, not
# a secret, and `pnpm generate` is unusable without knowing it exists.
!.env.production.example
