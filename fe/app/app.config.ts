export default defineAppConfig({
  ui: {
    // Points Nuxt UI's semantic colours at the palette declared in
    // assets/css/main.css, otherwise components fall back to its green default.
    colors: {
      primary: 'primary',
      secondary: 'accent',
      neutral: 'neutral',
    },
  },
})
